const {ObjectID} = require('mongodb')
const {mongoose} = require('./../server/db/mongoose')
const {Todo} = require('./../ervers/models/todo')


// if(!ObjectID.isValid("1")){
//   console.log('Id not valid')
// } else {
//   Todo.findById()
// }
